// Figma landing typography — Bebas Neue (display) + Space Mono (body)
// Defined here to avoid depending on ui package build; fonts loaded via index.html.
export const LANDING_FONT_FAMILIES = {
  display: 'Bebas Neue, sans-serif',
  body: '"Space Mono", monospace',
} as const
